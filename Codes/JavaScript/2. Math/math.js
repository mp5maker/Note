/**
 * Give any decimal value between 0 to 1
 */
var random = Math.random();
console.log(random);

/**
 * Give any decimal value between 0 to 5
 */
var random_five = (5 * Math.random());
console.log(random_five);

/**
 * Math Ceil
 */
console.log(Math.ceil(random_five));

/**
 * Math Floor
 */
console.log(Math.floor(random_five));

/**
 * Math Square Root
 */
var number = 9;
console.log(Math.sqrt(number));

/**
 * Math PI
 */
radius = 3;
console.log("Area of the Circle: " + (Math.PI * Math.pow(3,2)))
